public class Enums {

    public enum Preference {
        ADVENTURE, RELAX, CULTURE
    }

    public enum Skill {
        HIKING, HISTORY_KNOWLEDGE, SCUBA_DIVING
    }

    public enum PaymentStatus {
        PENDING, COMPLETED, CANCELLED
    }
}